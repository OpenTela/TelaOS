# RULE: Версионирование модулей

## Принцип

Каждый модуль имеет версию. Версия выводится в лог при инициализации.

## Формат версии

`MAJOR.MINOR.PATCH` (семантическое версионирование)

- **MAJOR** — несовместимые изменения API
- **MINOR** — новая функциональность (обратно совместимая)
- **PATCH** — исправления багов

## Как добавить версию в модуль

### C++ (ESP32)

```cpp
namespace MyModule {
    static const char* TAG = "MyModule";
    static const char* VERSION = "1.0.0";
    
    void init() {
        ESP_LOGI(TAG, "MyModule v%s initialized", VERSION);
    }
}
```

### Python

```python
VERSION = "1.0.0"

print(f"🚀 MyApp v{VERSION}")
```

## Текущие версии

Актуальные версии модулей выводятся в Serial при запуске. Пример:
```
[BLEBridge] BLEBridge v2.0.0
[LuaFetch] LuaFetch v2.0.0 - fetch() and ble_connected() registered
```

## Зачем это нужно

1. **Отладка**: сразу видно какой код работает
2. **Контроль**: понятно обновился ли модуль
3. **История**: трекинг изменений

## Пример лога

```
[BLEBridge] ========================================
[BLEBridge]   BLE Device: FutureClock
[BLEBridge]   BLEBridge v2.0.0
[BLEBridge]   MAC Address: dc:b4:d9:20:80:f1
[BLEBridge] ========================================
...
[LuaFetch] LuaFetch v2.0.0 - fetch() and ble_connected() registered
```

Если в логе версия **не 2.0.0** — код не обновился!
